global function Interact()
	print ("examine crashed car script","\n")
	local doctor = GetActor("doctor")

	SayPrep("skip", "")
	
	--VO
	--Examine crashed taxi
		doctor::SID_0553:This won't be taking any fares south of the river again.
end
