global function Interact()
	local doctor = GetActor("doctor")
	
	--VO
	--Examine panel (broken and blocking return).
		doctor::SID_2218:Another broken panel. But there is no reason to go back down anyway.
end

global function Use()
	local doctor = GetActor("doctor")

	--VO
	--Use Sonic on panel (broken and blocking return).
		doctor::SID_2219:Amy is further up these lifts. I shouldn't go back down.
end
