global function Trigger()
	local doctor = GetActor("doctor")
	
	--VO
	--Prompt to turn round
		doctor::SID_2231:There is no way past this guard. There must be another way down through these tunnels.
end
