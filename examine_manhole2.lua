global function Interact()
	local doctor = GetActor("doctor")

	SayPrep("skip", "")

	--VO
	--Examine manhole (used)
		doctor::SID_0504:There are still Daleks down there. We need to get back to the TARDIS.
end
